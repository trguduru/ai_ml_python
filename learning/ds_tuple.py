# examples about tuple data structure , tuples are immutable, not sorted
dimensions = 10, 20, 30
print(dimensions[0], dimensions[1], dimensions[2])
length, width, height = dimensions
print(length, width, height)
